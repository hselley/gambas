' Gambas module file

Public Sub Main()
  Dim i, j, k, m, n As Short
  
  Print "Ingrese dos numeros positivos"
  Input m, n
  
  If m > 0 And n > 0 Then
    k = 1
    For i = 1 To m 
      For j = 1 To n
        Print k;;
        If k == 9 Then
          k = 0
        Else 
          Inc k 'k=k+1  
        Endif        
      Next
      Print 
    Next
  Endif
End
